for (let a = 0; a < 2; a++) {
  for (let b = 0; b < 2; b++) {
    for (let c = 0; c < 2; c++) {
      for (let d = 0; d < 2; d++) {
        for (let e = 0; e < 2; e++) {
          for (let f = 0; f < 2; f++) {
            for (let g = 0; g < 2; g++) {
              for (let h = 0; h < 2; h++) {
                if (a <= 1 && b <= 1 && c <= 1) {
                  console.log(a + ' ' + b + ' ' + c + ' ' + d + ' ' + e + ' ' + f + ' ' + g + ' ' + h);
                }
              }
            }
          }
        }
      }
    }
  }
}