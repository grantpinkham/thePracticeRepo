const jsArray = ['J',
                 'a',
                 'v',
                 'a',
                 'S',
                 'c',
                 'r',
                 'i',
                 'p',
                 't'
];
const jsLength = jsArray.length;
for (let i = 0; i < 1; i++) {
  const newArray = jsArray.join('');
  console.log(newArray);
}
